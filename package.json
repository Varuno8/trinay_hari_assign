{
  "name": "transcript-evaluator",
  "version": "1.0.0",
  "description": "Evaluate AI coding session transcripts using LLM",
  "scripts": {
    "build": "tsc -p src/tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsc -p src/tsconfig.json && node dist/server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
